export interface ButtonCarouselProps {
    direction: string,
    moveSlide: () => void,
  }
  export interface DataSliderProps {
    id: number;
    title: string;
    subTitle: string;
    img: any;
  }